defaultNamespace: crossplane-system

helm:
  chart: crossplane
  repo: https://charts.crossplane.io/stable
  #version: 1.10.0

targetCustomizations:
- name: prod
  yaml:
    values:
      - prodValues
  clusterSelector:
    matchLabels:
      env: prod
