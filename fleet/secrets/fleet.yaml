defaultNamespace: default

targetCustomizations:

- name: dev
  yaml:
    Values:
    - dev
  clusterSelector:
    matchLabels:
      env: dev
      
- name: prod
  yaml:
    Values:
    - prod
  clusterSelector:
    matchLabels:
      env: prod
